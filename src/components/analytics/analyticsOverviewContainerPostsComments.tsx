import React from "react";
import styles from "../../styles/components/analytics/analyticsOverviewContainerPostsComments.module.css";

interface analyticsOverviewContainerPostsCommentsProps{
    postId: number;
}

const AnalyticsOverviewContainerPostsComments: React.FC<analyticsOverviewContainerPostsCommentsProps> = ({postId}) => {
    return(
        <div>
            Hi2!
        </div>
    );
};

export default AnalyticsOverviewContainerPostsComments
